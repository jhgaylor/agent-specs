{
  "name": "agent-specs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && chant build --output dist/fountain.yaml --format json",
    "apply": "npm run build && npx tsx scripts/apply.mjs"
  },
  "devDependencies": {
    "@intentius/chant": "^0.33.1",
    "@intentius/chant-lexicon-fountain": "^0.33.1",
    "typescript": "^5.9.3",
    "yaml": "^2.9.0"
  }
}
