{
  "name": "agent-specs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && chant build --output dist/fountain.yaml --format json && npx tsx scripts/check-names.mjs",
    "apply": "npm run build && npx tsx scripts/apply.mjs"
  },
  "devDependencies": {
    "@intentius/chant": "^0.44.14",
    "@intentius/chant-lexicon-fountain": "^0.44.14",
    "typescript": "^5.9.3"
  }
}
